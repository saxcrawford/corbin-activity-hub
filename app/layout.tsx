import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
    title: "Corbin Activity Hub - Discover Corbin, Kentucky",
    description: "Discover activities, plan trips, and learn about Corbin, Kentucky - home of the original KFC and the moonbow at Cumberland Falls.",
    keywords: ["Corbin", "Kentucky", "Travel", "Tourism", "Cumberland Falls", "KFC"],
};


export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
